[//]: # (excode-config: mode=chicken)

This readme has a valid excode header but an unsupported language mode. 
When the code block is extracted, it will fail in writing since the language isn't supported.

```chicken
chicken
```
